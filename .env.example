VITE_API_BASE_URL=/v1

# OpenAI — frontend only. Put base64(key) here (keeps value on GitHub for server pull).
# PowerShell: [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("sk-..."))
VITE_OPENAI_API_KEY_B64=
VITE_OPENAI_MODEL=gpt-4o-mini
